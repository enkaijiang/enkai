# AGENTS.md

## Cursor Cloud specific instructions

This repository (`enkai`) is currently an empty skeleton — single `README.md`, one initial commit, no application code, no dependencies, and no services.

- **No build/test/lint/run commands exist yet.** Future agents should re-evaluate once application code is added.
- **No package manager lockfile is present.** When one is added, the update script should be adjusted accordingly.
- **No services to start.** No database, no backend, no frontend at this time.

When the codebase gains actual application code, update this section with:

1. How to install dependencies (package manager + command).
2. How to run the dev server(s).
3. How to run lint and tests.
4. Any non-obvious caveats discovered during setup.
